'use client';
import React from 'react';

const TimeslotClasses = ({ timeslot, date} : { timeslot : number, date: Date }) => {
    return (
        <div className="h-[30px] text-black flex items-center text-center justify-center border border-black">
          Test
        </div>
    );
};

export default TimeslotClasses;
